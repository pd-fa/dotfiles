[tools]
node = "22"
python = "3.13"
go = "1.24"
rust = "stable"
# Tool, not a runtime — pinned so two machines resolve the same uv.
uv = "0.12"

[settings]
# Trust project-level .mise.toml only after explicit `mise trust`
experimental = false
